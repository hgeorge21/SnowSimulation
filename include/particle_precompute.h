#ifndef PARTICLE_PRECOMPUTE_H
#define PARTICLE_PRECOMPUTE_H

#include <Eigen/Core>
#include <grid.h>
#include <particles.h>

void particle_precompute(Grid& grid, Particle& particles);

#endif // !PARTICLE_PRECOMPUTE_H
